{{render 'genres'}}

<ul class="movies-list">
  {{#each controller.arrangedContent}}
    <li>
      {{#linkTo 'movie' this}}
        {{title}}
        <i class="icon-chevron-right"></i>
      {{/linkTo}}
    </li>
  {{/each}}
</ul>

{{outlet}}
