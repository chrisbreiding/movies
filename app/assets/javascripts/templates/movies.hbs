<h1>Movies</h1>

<ul class="movies-list">
  {{#each model}}
    <li>
      {{#linkTo 'movie' this}}
        {{title}}
        <i class="icon-arrow-right"></i>
      {{/linkTo}}
    </li>
  {{/each}}
</ul>

{{outlet}}
